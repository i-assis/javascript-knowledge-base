
/** 
* * PROBLEM -   
* ? Write a function that performs
* ? the four basic operations over any given
* ? pair of numbers at the input.
* ?
* ? 
**/

function calcularOperacoes (operador1, operador2) {
    console.log(
        operador1+operador2, 
        operador1-operador2,
        operador1*operador2, 
        operador1/operador2
        );
}

calcularOperacoes(1, 2)

sdf
